export function get_translation(slug, container=undefined, state=undefined, params=undefined, defaultSlug=undefined) {

    /*
    const c = state || this.core;
    if (!c) { 
        return slug;
    }

    const localizations = c.localizations;
    const defaultContainer = localizations.defaultContainer;
    const containers = localizations.containers;

    const i = container || defaultContainer;
    const targetContainer = containers[i];

    const stringSlug = slug.toString().trim();

    let translatedText = "";
    if (defaultString && !targetContainer[stringSlug]) {
        translatedText = 
    */

   return slug;
}
